---

- hosts: all
  gather_facts: no

# CONCORD SERVER INSTANCES

- hosts: servers
  roles:
    - concord-server
    - concord-console

# CONCORD AGENT INSTANCES

- hosts: agents
  roles:
    - concord-agent
